'use strict';

{
  // 配列

  const scores = [80, 90, 40];
番号で指定する
  // console.log(scores[1]); // 90
// 指定して内容を変える
  // scores[2] = 100;
  // console.log(scores);
// 配列の中身は変更できても再代入はできない
  // scores = 1;
// 配列の数
  console.log(scores.length); // 3
}
